{
  "name": "scholarsync",
  "version": "1.0.0",
  "private": true,
  "description": "ScholarSync — Intelligent Faculty Mentorship & Scheduling Platform",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "db:migrate": "npm run db:migrate --prefix server",
    "db:seed": "npm run db:seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
